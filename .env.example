# Example environment file for docker-compose
# Copy this to .env and modify the values as needed

# MQTT Broker Settings
MQTT_BROKER=192.168.1.100
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_TOPIC=home/wake

# Logging Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Configuration file path (usually don't need to change)
CONFIG_FILE=/app/config.yaml